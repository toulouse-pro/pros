const SCHEMA = {
  accueil: {
    file: "_data/accueil.yml", // Make sure this path matches your GitHub exactly
    fields: [
      { name: "titre", label: "Titre", type: "text" },
      { name: "description", label: "Description", type: "textarea" }
    ]
  }
};
